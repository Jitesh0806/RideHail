# API URL - set to your local IP when backend is running
# Find your IP: ipconfig (Windows) or ifconfig (Mac/Linux)
EXPO_PUBLIC_API_URL=http://192.168.1.100:3000

# For demo mode (no backend), leave blank - app works fully without it
